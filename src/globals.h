#ifndef METALTOY_COMMON_H
#define METALTOY_COMMON_H

// Global variables. Meant to be set once at start and then only read from then
// on. The are defined in main.cpp

extern unsigned int global_texture_width;
extern unsigned int global_texture_height;

#endif
